# Quiz Import System - Полное руководство

Система импорта квизов для Quiz Sprint с поддержкой **3 форматов**, **тегов**, **LLM-генерации** и **batch-импорта**.

---

## 📚 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Форматы файлов](#-форматы-файлов)
3. [Генерация с помощью LLM](#-генерация-с-помощью-llm)
4. [Система тегов](#-система-тегов)
5. [JSON Schema](#-json-schema)
6. [Примеры](#-примеры)
7. [Валидация и ошибки](#-валидация-и-ошибки)
8. [FAQ](#-faq)

---

## 🚀 Быстрый старт

### Импорт одного квиза

```bash
# Компактный формат (рекомендуется)
./bin/import-quiz -file data/quizzes/templates/TEMPLATE-COMPACT.json

# Старый формат (для совместимости)
./bin/import-quiz -file data/quizzes/legacy/programming-basics.json
```

### Импорт batch (10+ квизов одновременно)

```bash
./bin/import-quiz -file data/quizzes/batches/2024-01/go-fundamentals.json
```

### Валидация без импорта (dry-run)

```bash
./bin/import-quiz -file data/quizzes/my-quiz.json -dry-run
```

### Импорт всех файлов из директории

```bash
./bin/import-quiz -dir data/quizzes/batches/2024-01
```

---

## 📁 Структура директорий

```
data/quizzes/
├── templates/          # Примеры всех форматов
│   ├── TEMPLATE.json          # Verbose формат (старый)
│   ├── TEMPLATE-COMPACT.json  # Compact формат (LLM-оптимизированный)
│   └── TEMPLATE-BATCH.json    # Batch формат (массовый импорт)
│
├── batches/           # Batch-файлы (рекомендуется)
│   ├── 2024-01/
│   │   ├── go-fundamentals.json    # 5 квизов по основам Go
│   │   └── go-advanced-10q.json    # 5 квизов по 10 вопросов
│   └── 2024-02/
│       └── ...
│
├── legacy/            # Старые файлы (verbose формат)
│   ├── programming-basics.json
│   ├── javascript-advanced.json
│   └── ...
│
└── README.md          # Этот файл
```

---

## 📝 Форматы файлов

Система автоматически определяет формат при импорте.

### 1. Verbose формат (Legacy) 📄

**Использование:** Старый формат с полными именами полей
**Токенов на квиз:** ~640
**Файл-пример:** `templates/TEMPLATE.json`

```json
{
  "title": "Quiz Title",
  "description": "Description",
  "categoryId": null,
  "timeLimit": 600,
  "passingScore": 70,
  "tags": ["language:go", "difficulty:easy"],
  "questions": [
    {
      "text": "Question text?",
      "points": 10,
      "answers": [
        { "text": "Answer 1", "isCorrect": false },
        { "text": "Answer 2", "isCorrect": true },
        { "text": "Answer 3", "isCorrect": false }
      ]
    }
  ]
}
```

### 2. Compact формат (LLM-оптимизированный) ⚡

**Использование:** Генерация квизов с помощью LLM
**Токенов на квиз:** ~230 (**экономия 64%!**)
**Файл-пример:** `templates/TEMPLATE-COMPACT.json`

```json
{
  "t": "Go Basics: Variables",
  "d": "Test Go fundamentals",
  "cat": "programming",
  "tags": ["language:go", "difficulty:easy", "topic:variables"],
  "l": 120,
  "p": 75,
  "q": [
    {
      "t": "What keyword declares a variable?",
      "a": ["let", "var", "const", "dim"],
      "c": 1
    }
  ]
}
```

**Mapping полей:**
- `t` → title (название)
- `d` → description (описание)
- `cat` → category (категория, опционально)
- `tags` → массив тегов
- `l` → timeLimit (по умолчанию 60 сек)
- `p` → passingScore (по умолчанию 70%)
- `q` → questions (вопросы)
- `a` → answers (варианты ответа, массив строк)
- `c` → correctIndex (индекс правильного ответа, 0-based)

### 3. Batch формат (Массовый импорт) 🚀

**Использование:** Импорт 10+ квизов с общими метаданными
**Токенов на квиз:** ~210 (**экономия 67%!**)
**Файл-пример:** `templates/TEMPLATE-BATCH.json`

```json
{
  "batch": {
    "version": 1,
    "generated": "2024-01-20T10:00:00Z",
    "cat": "programming",
    "tags": ["language:go", "difficulty:easy"]
  },
  "quizzes": [
    {
      "t": "Quiz 1",
      "d": "Description",
      "tags": ["topic:variables"],
      "q": [...]
    },
    {
      "t": "Quiz 2",
      "d": "Description",
      "tags": ["topic:functions"],
      "l": 90,
      "q": [...]
    }
  ]
}
```

**Особенности:**
- Общие теги из `batch.tags` + теги квиза
- Общая категория `batch.cat` (можно переопределить в квизе)
- Автоматическая генерация batch ID
- Отслеживание времени генерации

---

## 🤖 Генерация с помощью LLM

### Вариант 1: Генерация прямо в проекте

Я (Claude) могу сгенерировать квизы прямо сейчас! Просто скажите:

```
"Сгенерируй 5 квизов про [тема] на [язык],
каждый по [N] вопросов, сложность [easy/medium/hard]"
```

**Примеры:**
- "Сгенерируй 5 квизов про Python на русском, каждый по 10 вопросов, сложность easy"
- "Создай квизы по истории России средних веков, по 8 вопросов"
- "Нужны квизы про JavaScript advanced, сложность hard"

### Вариант 2: Claude.ai / ChatGPT

#### Шаг 1: Скопируйте промпт

```
Сгенерируй 5 квизов про [ТЕМА] на [ЯЗЫК] в компактном JSON формате.

Требования:
- Каждый квиз: ровно 10 вопросов
- Каждый вопрос: ровно 4 варианта ответа
- Только ОДИН правильный ответ на вопрос
- "c" = индекс правильного ответа (0, 1, 2 или 3)
- Разные подтемы в каждом квизе
- Используй теги: language:[язык], difficulty:[easy/medium/hard], topic:[конкретная тема]

Формат:

{
  "batch": {
    "version": 1,
    "generated": "2024-01-20T10:00:00Z",
    "cat": "programming",
    "tags": ["language:go", "difficulty:medium"]
  },
  "quizzes": [
    {
      "t": "Название квиза",
      "d": "Описание (1-2 предложения)",
      "tags": ["topic:конкретная-тема"],
      "l": 120,
      "p": 75,
      "q": [
        {
          "t": "Текст вопроса?",
          "a": ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
          "c": 1
        }
        // ... ещё 9 вопросов
      ]
    }
    // ... ещё 4 квиза
  ]
}

ВАЖНО:
- Верни только валидный JSON (без markdown, без комментариев)
- Проверь, что в каждом вопросе c = 0, 1, 2 или 3
- Проверь, что в каждом квизе ровно 10 вопросов
```

#### Шаг 2: Готовые промпты по темам

**Go Programming (средняя сложность)**
```
Сгенерируй 5 квизов про Go programming на английском.

Темы:
1. Concurrency (goroutines, channels)
2. Interfaces and Methods
3. Pointers and Memory
4. Testing and Benchmarking
5. Packages and Modules

Каждый квиз: 10 вопросов, 4 варианта ответа, сложность medium.
Теги: language:go, difficulty:medium, topic:[тема]
Время: 120 секунд, проходной балл: 75%
```

**Python для начинающих**
```
Сгенерируй 5 квизов про Python для начинающих на русском.

Темы:
1. Типы данных и переменные
2. Условия и циклы
3. Функции
4. Списки и словари
5. Работа с файлами

Каждый квиз: 10 вопросов, 4 варианта ответа, сложность easy.
Теги: language:python, difficulty:easy, topic:[тема]
Время: 90 секунд, проходной балл: 70%
```

**История России (сложный)**
```
Сгенерируй 5 квизов по истории России на русском.

Темы:
1. Древняя Русь (IX-XIII века)
2. Московское царство (XV-XVII века)
3. Российская империя (XVIII-XIX века)
4. СССР (1922-1991)
5. Современная Россия (1991-настоящее)

Каждый квиз: 10 вопросов, 4 варианта ответа, сложность hard.
Теги: domain:history, difficulty:hard, topic:[период]
Время: 180 секунд, проходной балл: 80%
```

#### Шаг 3: Импорт

```bash
# Сохраните JSON от LLM в файл
nano data/quizzes/batches/2024-01/my-batch.json

# Импортируйте
./bin/import-quiz -file data/quizzes/batches/2024-01/my-batch.json
```

### 📊 Экономия токенов

| Формат | Токенов/квиз | 100 квизов | Экономия |
|--------|--------------|------------|----------|
| Verbose | ~640 | 64,000 | - |
| Compact | ~230 | 23,000 | 64% |
| Batch (10 шт) | ~210 | 21,000 | 67% |

**Стоимость (Claude Opus ~$0.02/1K токенов):**
- Verbose: ~$1.28
- Compact: ~$0.46 → **экономия $0.82!**

---

## 🏷️ Система тегов

Теги обеспечивают гибкую категоризацию и фильтрацию квизов.

### Формат тега

```
{category}:{value}
```

**Примеры:**
- `language:go`
- `difficulty:easy`
- `topic:web-development`
- `domain:history`

### Доступные категории

| Категория | Описание | Примеры значений |
|-----------|----------|------------------|
| `language:*` | Языки программирования | go, python, javascript, rust, java, cpp |
| `difficulty:*` | Сложность квиза | easy, medium, hard, expert |
| `topic:*` | Конкретные темы | variables, functions, concurrency, testing |
| `domain:*` | Общие домены | programming, history, science, movies, geography |
| `format:*` | Формат квиза | multiple-choice, true-false |

### Правила валидации

- ✅ Только lowercase (маленькие буквы)
- ✅ Максимум 100 символов
- ✅ Паттерн: `^[a-z0-9-:]+$`
- ✅ 1-10 тегов на квиз
- ✅ Для multi-word используйте дефис: `web-development`
- ❌ НЕ используйте пробелы: `web development`

### Автоматический вывод категории

Если поле `cat` не указано, категория выводится из тегов:

**Приоритет 1: language теги**
- `language:go` → `programming`
- `language:python` → `programming`
- `language:javascript` → `programming`

**Приоритет 2: domain теги**
- `domain:history` → `history`
- `domain:science` → `science`
- `domain:movies` → `movies`
- `domain:geography` → `geography`

**Fallback:** `general`

**Пример:**
```json
{
  "t": "Go Basics",
  "tags": ["language:go", "difficulty:easy"]
  // cat автоматически: "programming"
}
```

---

## 📋 JSON Schema

### Verbose формат (полная схема)

```json
{
  "title": "string (required, 3-200 characters)",
  "description": "string (optional)",
  "categoryId": "string (optional, UUID format)",
  "tags": ["array of strings (optional, 1-10 tags)"],
  "timeLimit": "integer (required, seconds, must be > 0)",
  "passingScore": "integer (required, percentage 0-100)",
  "questions": [
    {
      "text": "string (required, 5-500 characters)",
      "points": "integer (required, must be > 0)",
      "answers": [
        {
          "text": "string (required, 1-200 characters)",
          "isCorrect": "boolean (required)"
        }
      ]
    }
  ]
}
```

### Compact формат (краткая схема)

```json
{
  "v": "integer (optional, version, default: 1)",
  "t": "string (required, title)",
  "d": "string (optional, description)",
  "cat": "string (optional, category name)",
  "tags": ["array of strings (optional)"],
  "l": "integer (optional, timeLimit in seconds, default: 60)",
  "p": "integer (optional, passingScore %, default: 70)",
  "q": [
    {
      "t": "string (required, question text)",
      "a": ["array of strings (required, 2+ answers)"],
      "c": "integer (required, correct answer index 0-based)",
      "p": "integer (optional, points, default: 10)"
    }
  ]
}
```

### Правила валидации

1. **Title**: 3-200 символов, обязательно
2. **TimeLimit**: Положительное число (> 0), по умолчанию 60
3. **PassingScore**: 0-100, по умолчанию 70
4. **Questions**: Минимум 1 вопрос
5. **Answers**:
   - Минимум 2 варианта ответа
   - Ровно 1 правильный ответ
   - В compact формате: `c` должен быть от 0 до `len(answers)-1`
6. **Tags**: 1-10 тегов, валидный формат
7. **Question Text**: 5-500 символов
8. **Answer Text**: 1-200 символов

---

## 💡 Примеры

### Пример 1: Простой квиз (Compact формат)

```json
{
  "t": "JavaScript Basics",
  "d": "Test your JS fundamentals",
  "tags": ["language:javascript", "difficulty:easy"],
  "q": [
    {
      "t": "What is the correct syntax for a comment?",
      "a": ["# comment", "// comment", "/* comment", "-- comment"],
      "c": 1
    },
    {
      "t": "Which keyword declares a constant?",
      "a": ["var", "let", "const", "final"],
      "c": 2
    }
  ]
}
```

### Пример 2: Batch файл (5 квизов)

```json
{
  "batch": {
    "version": 1,
    "cat": "programming",
    "tags": ["language:python", "difficulty:medium"]
  },
  "quizzes": [
    {
      "t": "Python Lists",
      "d": "Master Python list operations",
      "tags": ["topic:data-structures"],
      "l": 90,
      "q": [
        {
          "t": "How do you create an empty list?",
          "a": ["list()", "[]", "Both", "None"],
          "c": 2
        }
      ]
    },
    {
      "t": "Python Dictionaries",
      "d": "Learn dict operations",
      "tags": ["topic:data-structures"],
      "q": [
        {
          "t": "How do you access dict values?",
          "a": ["dict.value", "dict[key]", "dict->key", "dict.get"],
          "c": 1
        }
      ]
    }
  ]
}
```

### Пример 3: Verbose формат (полный)

```json
{
  "title": "Programming Basics",
  "description": "Test your knowledge of fundamental programming concepts",
  "categoryId": null,
  "tags": ["domain:programming", "difficulty:easy"],
  "timeLimit": 600,
  "passingScore": 70,
  "questions": [
    {
      "text": "What does HTML stand for?",
      "points": 10,
      "answers": [
        {
          "text": "Hyper Text Markup Language",
          "isCorrect": true
        },
        {
          "text": "High Tech Modern Language",
          "isCorrect": false
        },
        {
          "text": "Home Tool Markup Language",
          "isCorrect": false
        }
      ]
    }
  ]
}
```

---

## ⚠️ Валидация и ошибки

### Частые ошибки

#### 1. Нет правильного ответа
```
Error: question 1: exactly one answer must be correct (found 0)
```
**Решение:** Убедитесь, что один ответ имеет `"isCorrect": true` или `"c"` указывает на валидный индекс.

#### 2. Несколько правильных ответов
```
Error: question 2: exactly one answer must be correct (found 2)
```
**Решение:** Только один ответ должен быть правильным.

#### 3. Индекс вне диапазона (Compact формат)
```
Error: correctIndex 4 out of range for 4 answers
```
**Решение:** `c` должен быть от 0 до 3 для 4 вариантов ответа.

#### 4. Невалидный формат тега
```
Error: invalid tag 'Language:Go': tag name must follow format {category}:{value}
```
**Решение:** Используйте lowercase: `language:go`

#### 5. Категория не найдена
```
Warning: category not found: java (quiz will have no category)
```
**Решение:** Используйте существующие категории или добавьте новую через SQL.

### Проверка перед импортом

```bash
# Dry-run для проверки валидности
./bin/import-quiz -file my-quiz.json -dry-run

# Если успешно:
✓ Title: My Quiz
✓ Questions: 10
✓ Time Limit: 120 seconds
✓ Passing Score: 75%
(Dry run - no data was imported)
```

---

## 📚 FAQ

### Q: Можно ли генерировать квизы на русском языке?
**A:** Да! LLM отлично генерирует квизы на любом языке. Просто укажите в промпте нужный язык.

### Q: Сколько квизов можно сгенерировать за раз?
**A:** Рекомендуется 5-10 квизов в одном batch файле. Больше - LLM может начать терять качество.

### Q: Как изменить количество вопросов в квизе?
**A:** В промпте для LLM укажите "каждый по [N] вопросов". В JSON можете добавить/удалить элементы массива `q`.

### Q: Что делать, если LLM вернул невалидный JSON?
**A:**
1. Скопируйте ошибку валидации
2. Попросите LLM исправить: "Исправь JSON, ошибка: [текст ошибки]"
3. Или исправьте вручную в редакторе

### Q: Можно ли редактировать уже импортированные квизы?
**A:** Да, через SQL или создайте обновленный JSON и импортируйте снова (используйте тот же ID).

### Q: Как удалить неудачный квиз?
**A:**
```sql
DELETE FROM quizzes WHERE title = 'Название';
-- или
DELETE FROM quizzes WHERE id = 'uuid';
```

### Q: Какой формат использовать?
**A:**
- **Compact** - для LLM генерации (экономия токенов)
- **Batch** - для массового импорта (10+ квизов)
- **Verbose** - для ручного создания или legacy совместимости

### Q: Сколько времени давать на квиз?
**A:** Базовая формула: `(количество_вопросов × 60) + 60` секунд
- 5 вопросов: 360 сек (6 мин)
- 10 вопросов: 660 сек (11 мин)
- 15 вопросов: 960 сек (16 мин)

### Q: Какой проходной балл устанавливать?
**A:**
- **Easy**: 60-70%
- **Medium**: 70-75%
- **Hard**: 75-85%
- **Expert**: 85-95%

---

## 🎯 Советы и рекомендации

### Организация файлов

```
batches/
├── 2024-01/
│   ├── programming-go.json       # 5-10 квизов по Go
│   ├── programming-python.json   # 5-10 квизов по Python
│   └── history-russia.json       # 5-10 квизов по истории
├── 2024-02/
│   └── ...
```

**Рекомендации:**
- Группируйте по языку/теме
- Используйте месячные директории
- Naming: `{domain}-{topic}.json`

### Качество вопросов

При генерации через LLM попросите:
- ✅ Избегать двусмысленных вопросов
- ✅ Делать неправильные ответы правдоподобными
- ✅ Проверять фактическую точность
- ✅ Разнообразить типы вопросов:
  - Определения ("Что такое...?")
  - Примеры ("Какой результат...?")
  - Сравнения ("В чём разница...?")
  - Применение ("Как использовать...?")

### Token optimization

- **Используйте Compact формат** для LLM генерации
- **Группируйте** похожие квизы в batch
- **Переиспользуйте** общие теги в `batch.tags`
- **Опускайте** default значения (`l: 60`, `p: 70`)

### Version control

```bash
# Добавьте batch файлы в Git
git add data/quizzes/batches/2024-01/*.json
git commit -m "Add Go programming quiz batch"

# Поделитесь с командой
git push
```

---

## 🔗 Дополнительные ресурсы

- **Backend IMPORT.md** - Полная документация по импорту
- **Templates** - Примеры всех форматов в `templates/`
- **Examples** - Готовые квизы в `legacy/` и `batches/`
- **LLM Generation Guide** - `backend/docs/LLM_GENERATION_GUIDE.md`

---

## 📊 Текущая статистика базы

Посмотреть количество квизов:
```bash
docker compose -f docker-compose.dev.yml exec -T postgres psql -U quiz_user -d quiz_sprint_dev \
  -c "SELECT COUNT(*) as quizzes, SUM((SELECT COUNT(*) FROM questions WHERE quiz_id = q.id)) as questions FROM quizzes q;"
```

Посмотреть теги:
```bash
docker compose -f docker-compose.dev.yml exec -T postgres psql -U quiz_user -d quiz_sprint_dev \
  -c "SELECT name, COUNT(*) as usage FROM tags t JOIN quiz_tags qt ON t.id = qt.tag_id GROUP BY name ORDER BY usage DESC;"
```

---

**Готово к использованию!** 🎉

Если нужна помощь - просто попросите сгенерировать квизы по вашей теме!
