# i18n (EN + RU) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add English + Russian i18n to the Vue 3 TMA using `vue-i18n@11`, auto-detecting locale from Telegram user settings with manual override in Profile.

**Architecture:** Install `vue-i18n@11`, create translation files in `src/i18n/locales/{en,ru}.ts`, wire up `createI18n` in `main.ts`, connect `UApp :locale` prop to Nuxt UI built-in locales, replace all 380+ hardcoded strings in 27 Vue files with `t('key')`, add `USelect` language switcher in `ProfileView.vue`.

**Tech Stack:** `vue-i18n@11` (Composition API mode, `legacy: false`), `@nuxt/ui/locale` (built-in `en`/`ru`), Vitest for locale detection tests

---

## Task 1: Install vue-i18n@11 and write locale detection tests

**Files:**
- Modify: `tma/package.json` (dependency added by pnpm)
- Create: `tma/src/i18n/index.ts`
- Create: `tma/src/__tests__/i18n.spec.ts`

**Step 1: Install package**

```bash
cd tma && pnpm add vue-i18n@11
```

Expected: `vue-i18n 11.x.x` in `package.json` dependencies.

**Step 2: Write failing tests for locale detection**

Create `tma/src/__tests__/i18n.spec.ts`:

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest'

// Mock @tma.js/sdk before importing the module under test
vi.mock('@tma.js/sdk', () => ({
  retrieveLaunchParams: vi.fn(),
}))

import { retrieveLaunchParams } from '@tma.js/sdk'
import { detectLocale } from '@/i18n/index'

describe('detectLocale', () => {
  beforeEach(() => {
    localStorage.clear()
    vi.mocked(retrieveLaunchParams).mockReturnValue({} as any)
  })

  it('returns localStorage value when set to ru', () => {
    localStorage.setItem('locale', 'ru')
    expect(detectLocale()).toBe('ru')
  })

  it('returns localStorage value when set to en', () => {
    localStorage.setItem('locale', 'en')
    expect(detectLocale()).toBe('en')
  })

  it('falls back to Telegram languageCode ru', () => {
    vi.mocked(retrieveLaunchParams).mockReturnValue({
      initData: { user: { languageCode: 'ru' } },
    } as any)
    expect(detectLocale()).toBe('ru')
  })

  it('normalizes ru-RU to ru', () => {
    vi.mocked(retrieveLaunchParams).mockReturnValue({
      initData: { user: { languageCode: 'ru-RU' } },
    } as any)
    expect(detectLocale()).toBe('ru')
  })

  it('falls back to en for unknown language', () => {
    vi.mocked(retrieveLaunchParams).mockReturnValue({
      initData: { user: { languageCode: 'de' } },
    } as any)
    expect(detectLocale()).toBe('en')
  })

  it('falls back to en when no Telegram data', () => {
    expect(detectLocale()).toBe('en')
  })

  it('localStorage takes priority over Telegram', () => {
    localStorage.setItem('locale', 'en')
    vi.mocked(retrieveLaunchParams).mockReturnValue({
      initData: { user: { languageCode: 'ru' } },
    } as any)
    expect(detectLocale()).toBe('en')
  })
})
```

**Step 3: Run tests to confirm they fail**

```bash
cd tma && pnpm test:unit src/__tests__/i18n.spec.ts
```

Expected: FAIL — `detectLocale` is not exported from `@/i18n/index`

**Step 4: Create `src/i18n/index.ts` with minimal implementation**

```typescript
import { createI18n } from 'vue-i18n'
import { retrieveLaunchParams } from '@tma.js/sdk'
import en from './locales/en'
import ru from './locales/ru'

export type Locale = 'en' | 'ru'

export function detectLocale(): Locale {
  // 1. Manual override
  const stored = localStorage.getItem('locale')
  if (stored === 'en' || stored === 'ru') return stored

  // 2. Telegram user language
  try {
    const params = retrieveLaunchParams()
    const lang = params?.initData?.user?.languageCode ?? ''
    if (lang.startsWith('ru')) return 'ru'
  } catch {
    // SDK not available outside Telegram
  }

  // 3. Default
  return 'en'
}

export const i18n = createI18n({
  legacy: false,
  locale: detectLocale(),
  availableLocales: ['en', 'ru'],
  messages: { en, ru },
})

export function setLocale(lang: Locale) {
  // @ts-expect-error — Composition API mode uses Ref<string>
  i18n.global.locale.value = lang
  localStorage.setItem('locale', lang)
}
```

**Step 5: Create `src/i18n/locales/en.ts`** (create empty stub for now)

```typescript
export default {}
```

**Step 6: Create `src/i18n/locales/ru.ts`** (create empty stub for now)

```typescript
export default {}
```

**Step 7: Run tests to confirm they pass**

```bash
cd tma && pnpm test:unit src/__tests__/i18n.spec.ts
```

Expected: All 7 tests PASS.

**Step 8: Commit**

```bash
git add tma/src/__tests__/i18n.spec.ts tma/src/i18n/index.ts tma/src/i18n/locales/en.ts tma/src/i18n/locales/ru.ts tma/pnpm-lock.yaml tma/package.json
git commit -m "feat(i18n): add locale detection with tests"
```

---

## Task 2: Fill in all English translation strings

**Files:**
- Modify: `tma/src/i18n/locales/en.ts`

**Step 1: Replace stub with complete English translations**

```typescript
// tma/src/i18n/locales/en.ts
export default {
  app: {
    loading: 'Loading...',
    error: 'Error',
    openInTelegram: 'App must be launched in Telegram',
    noAuthData: 'No authorization data from Telegram',
    registerFailed: 'Failed to register user',
    openInTelegramHint: 'Make sure the app is running in Telegram',
  },
  nav: {
    home: 'Home',
    leaderboard: 'Leaderboard',
    profile: 'Profile',
  },
  home: {
    todaysChallenge: "Today's Challenge",
    gameModes: 'Game Modes',
    partyMode: 'Party Mode',
    partyModeDesc: 'Multiplayer quiz party with friends',
    tournament: 'Tournament',
    tournamentDesc: 'Compete in weekly tournaments for prizes',
    comingSoon: 'Coming Soon',
  },
  quiz: {
    title: 'Quizzes',
    category: 'Category: {name}',
    loading: 'Loading quizzes...',
    loadError: 'Loading error',
    loadFailed: 'Failed to load quizzes',
    tryAgain: 'Try again',
    unnamed: 'Unnamed Quiz',
    noDescription: 'No description',
    questionsCount: '{count} questions',
    timeLimit: '{min} min',
    passingScore: '✅ Passing score: {score}%',
    viewQuiz: 'View Quiz',
    notFound: 'Quizzes not found',
    notFoundDesc: 'No quizzes in this category yet',
    backToCategories: 'Back to categories',
    stats: '📊 Quiz Stats',
    questions: 'Questions:',
    timeLimitLabel: 'Time Limit:',
    passingScoreLabel: 'Passing Score:',
    topLeaders: '👑 Top 3 Leaders',
    anonymous: 'Anonymous',
    startQuiz: 'START QUIZ →',
    resumingSession: 'Resuming session...',
    loadingQuiz: 'Loading quiz...',
    startingQuiz: 'Starting quiz...',
    questionOf: 'Question {current} of {total}',
    score: 'Score: {score}',
    streakLabel: '🔥 {count} Streak!',
    correct: '✓ Correct!',
    pointsEarned: '+{points} points',
    basePoints: 'Base: {points}',
    speedBonus: 'Speed Bonus: +{points} ⚡',
    streakBonus: 'Streak Bonus: +{points} 🔥',
    wrongAnswer: 'Better luck next time! Streak lost.',
    streakBonusTitle: '🔥 Streak Bonus!',
    streakBonusDesc: 'Amazing! {count} correct answers in a row! +{bonus} bonus points',
    submit: 'Submit Answer',
    submitting: 'Submitting...',
    loadingNext: 'Loading next question...',
    loadFailed2: 'Failed to load quiz',
    tryAgain2: 'Please try again',
    activeSession: 'Active Quiz Session Found',
    activeSessionDesc: 'You already have an active quiz session. Would you like to continue where you left off or start fresh?',
    continueSession: 'Continue Session',
    startFresh: 'Start Fresh',
    startingFresh: 'Starting Fresh...',
    loadingResults: 'Loading results...',
    loadResultsFailed: 'Failed to load results',
    completed: 'Quiz Completed!',
    passed: '✓ Passed',
    notPassed: '✗ Not Passed',
    correctAnswers: 'Correct Answers',
    timeSpent: 'Time Spent',
    pointsEarnedLabel: 'Points Earned',
    accuracy: 'Accuracy',
    longestStreak: 'Longest Streak',
    avgResponse: 'Avg. Response',
    tryAgainBtn: 'Try Again',
    viewLeaderboard: 'View Leaderboard',
    backToHome: 'Back to Home',
    shareAchievement: 'Share your achievement!',
    share: 'Share',
  },
  profile: {
    title: 'Profile',
    email: 'Email',
    notSet: 'Not set',
    language: 'Language',
    statistics: 'Statistics',
    quizzesCompleted: 'Quizzes Completed',
    totalPoints: 'Total Points',
    averageScore: 'Average Score',
    bestRank: 'Best Rank',
    achievements: 'Achievements',
    achievementsSoon: 'Achievement system coming soon!',
    recentActivity: 'Recent Activity',
    activitySoon: 'Activity history coming soon!',
  },
  leaderboard: {
    title: 'Leaderboard',
    subtitle: 'Top players by score',
    loading: 'Loading leaderboard...',
    loadFailed: 'Failed to load leaderboard',
    retry: 'Retry',
    empty: 'No scores yet!',
    emptyDesc: 'Be the first to complete this quiz.',
    yourRank: 'Your Rank',
    colRank: 'Rank',
    colPlayer: 'Player',
    colScore: 'Score',
    colDate: 'Date',
    anonymous: 'Anonymous',
    showingTop: 'Showing top {count} players',
  },
  categories: {
    title: 'Quiz Sprint',
    subtitle: 'Choose a category',
    loading: 'Loading categories...',
    loadError: 'Loading error',
    loadFailed: 'Failed to load categories',
    tryAgain: 'Try again',
    exploreDesc: 'Explore {name} quizzes',
    empty: 'Categories not available yet',
    emptyDesc: 'Make sure the backend is running',
  },
  marathon: {
    title: 'Marathon',
    score: 'Score',
    question: 'Question',
    recordLabel: '{score}/{best} record',
    gameOver: 'Game Over',
    questionsAnswered: '{count} questions answered',
    personalBest: 'Record',
    bonuses: 'Bonuses',
    energyRule1: '5 energy, error = −1 ⚡',
    energyRule2: '5 correct in a row = +1 ⚡',
    difficultyRule: 'Difficulty increases over time',
    continueMarathon: 'Continue Marathon',
    newRun: 'New Run',
    viewResults: 'View Results',
    startMarathon: 'Start Marathon',
    nextMilestone: 'Next milestone: {next} ({remaining} to go)',
    loadingQuestion: 'Loading question...',
    submittingAnswer: 'Submitting answer...',
    shieldAbsorbed: 'Shield absorbed the hit!',
    correct: 'Correct!',
    wrongLifeLost: 'Wrong! Life lost',
    incorrect: 'Incorrect',
    runCompleted: 'Run completed',
    correctAnswers: 'Correct answers',
    questions: 'Questions',
    newRecord: 'New record! 🎉',
    continueRun: 'Continue current run?',
    continueRunDesc: 'Get +1 ⚡ and continue from the same place',
    continueWithCoins: 'Continue ({coins} coins)',
    watchAd: 'Watch ad',
    home: 'Home',
    chooseCategory: 'Choose Category',
    chooseCategoryDesc: 'Select a category for your marathon',
    loadingCategories: 'Loading categories...',
    allCategories: 'All Categories',
    allCategoriesDesc: 'Questions from all topics',
    lives: '{count} lives',
  },
  daily: {
    title: "Today's Challenge",
    continue: 'Continue',
    viewResults: 'View Results',
    startChallenge: 'Start Challenge',
    questionsInfo: '10 questions • 15s each',
    resetsIn: 'Resets in',
    playersToday: 'Players Today',
    topPlayers: 'Top Players',
    todaysChallenge: "Today's daily challenge",
    noPlayers: 'No players yet',
    noPlayersDesc: "Be the first to complete today's challenge!",
    youBadge: 'You',
    points: 'points',
    loadingQuestion: 'Loading question...',
    submitting: 'Submitting answer...',
    correct: 'Correct!',
    incorrect: 'Incorrect',
    questionOf: '{current}/{total}',
    loadingResults: 'Loading results...',
    baseScore: 'Base: {score}',
    streakBonus: '+ {bonus} streak (×{multiplier})',
    correctOf: '{correct} / {total} correct',
    scorePercent: '({percent}%)',
    newStreakRecord: 'New Streak Record!',
    streakRecordDesc: "You've reached a {days} day streak!",
    yourRewards: 'Your Rewards',
    coins: 'Coins',
    pvpTickets: 'PVP Tickets',
    streakApplied: 'Streak ×{multiplier} applied to coins',
    marathonBonuses: 'Marathon Bonuses',
    shieldName: 'Shield',
    shieldDesc: 'Absorbs 1 wrong answer',
    fiftyfiftyName: '50/50',
    fiftyfiftyDesc: 'Removes 2 wrong answers',
    skipName: 'Skip',
    skipDesc: 'Skip without penalty',
    freezeName: 'Freeze',
    freezeDesc: '+5 seconds to timer',
    bonusPlus1: '+1',
    bonusUseDesc: 'Use in Solo Marathon mode',
    yourRanking: 'Your Ranking',
    yourRank: 'Your Rank',
    totalPlayers: 'Total Players',
    tryAgain: 'Try Again?',
    tryAgainDesc: "You can retry today's challenge to improve your score. Your best score will count for the leaderboard.",
    retryCoins: '100 Coins',
    retry: 'Retry',
    watchAd: 'Watch Ad',
    freeRetry: 'Free Retry',
    backToHome: 'Back to Home',
    nextChallenge: 'Next challenge available in',
    loadingReview: 'Loading review...',
    reviewTitle: 'Answer Review',
    reviewDesc: "Review your answers from today's challenge",
    correctLabel: 'Correct',
    wrongLabel: 'Wrong',
    backToResults: 'Back to Results',
    yourAnswer: 'Your Answer:',
    correctAnswer: 'Correct Answer:',
    pointsEarned: '+{points} points',
    noPoints: 'No points',
  },
  duel: {
    title: 'PvP Duel',
    acceptingChallenge: 'Accepting challenge...',
    pleaseLogin: 'Please log in',
    acceptFailed: 'Failed to accept challenge. Link may be expired.',
    close: 'Close',
    tickets: 'tickets',
    wins: 'Wins',
    losses: 'Losses',
    winRate: 'Win Rate',
    pendingChallenges: 'Pending Challenges',
    challenge: 'Challenge',
    accept: 'Accept',
    decline: 'Decline',
    tabPlay: 'Play',
    tabLeaderboard: 'Leaderboard',
    tabHistory: 'History',
    searching: 'Searching for opponent...',
    cancel: 'Cancel',
    findOpponent: 'Find Random Opponent',
    noTickets: 'No tickets available',
    challengeFriend: 'Challenge a Friend',
    inviteFriend: 'Invite Friend via Telegram',
    createLink: 'Create Challenge Link',
    shareLink: 'Share this link:',
    copy: 'Copy',
    friendsOnline: 'Friends Online',
    inGameBadge: 'In Game',
    yourRank: 'Your Rank',
    player: 'Player',
    wIndicator: 'W',
    lIndicator: 'L',
    noGames: 'No games yet',
    vsOpponent: 'vs {name}',
    friendBadge: 'Friend',
    pendingCount: '{count} challenges',
    mmrValue: '{mmr} MMR',
    thisSeason: 'This Season',
    totalGames: '{count} games',
    continueDuel: 'Continue Duel',
    challengesCount: 'Challenges ({count})',
    reconnecting: 'Reconnecting...',
    connecting: 'Connecting...',
    waitingOpponent: 'Waiting for opponent...',
    waitingDesc: 'The duel will start when both players are ready',
    getReady: 'Get ready!',
    you: 'You',
    round: 'Round',
    roundOf: '{current}/{total}',
    draw: 'Draw!',
    victory: 'Victory!',
    defeat: 'Defeat',
    mmrChange: '{sign}{amount} MMR',
    gameResult: 'Game Result',
    opponent: 'Opponent',
    friendGame: 'Friend Game',
    requestRematch: 'Request Rematch',
    shareResult: 'Share Result',
    backToLobby: 'Back to Lobby',
  },
  shared: {
    questionOf: 'Question {current} / {total}',
    pts: '{pts} pts',
    timesUp: "Time's up!",
  },
}
```

**Step 2: Verify TypeScript compiles**

```bash
cd tma && pnpm type-check
```

Expected: No errors.

**Step 3: Commit**

```bash
git add tma/src/i18n/locales/en.ts
git commit -m "feat(i18n): add English translation strings"
```

---

## Task 3: Fill in all Russian translation strings

**Files:**
- Modify: `tma/src/i18n/locales/ru.ts`

**Step 1: Replace stub with complete Russian translations**

```typescript
// tma/src/i18n/locales/ru.ts
export default {
  app: {
    loading: 'Загрузка...',
    error: 'Ошибка',
    openInTelegram: 'Приложение должно быть запущено в Telegram',
    noAuthData: 'Нет данных авторизации из Telegram',
    registerFailed: 'Не удалось зарегистрировать пользователя',
    openInTelegramHint: 'Убедитесь, что приложение запущено в Telegram',
  },
  nav: {
    home: 'Главная',
    leaderboard: 'Рейтинг',
    profile: 'Профиль',
  },
  home: {
    todaysChallenge: 'Вызов дня',
    gameModes: 'Режимы игры',
    partyMode: 'Вечеринка',
    partyModeDesc: 'Квиз-вечеринка с друзьями',
    tournament: 'Турнир',
    tournamentDesc: 'Участвуй в еженедельных турнирах за призы',
    comingSoon: 'Скоро',
  },
  quiz: {
    title: 'Квизы',
    category: 'Категория: {name}',
    loading: 'Загрузка квизов...',
    loadError: 'Ошибка загрузки',
    loadFailed: 'Не удалось загрузить квизы',
    tryAgain: 'Попробовать снова',
    unnamed: 'Без названия',
    noDescription: 'Нет описания',
    questionsCount: '{count} вопросов',
    timeLimit: '{min} мин',
    passingScore: '✅ Проходной балл: {score}%',
    viewQuiz: 'Открыть квиз',
    notFound: 'Квизы не найдены',
    notFoundDesc: 'В этой категории пока нет квизов',
    backToCategories: 'Вернуться к категориям',
    stats: '📊 Статистика квиза',
    questions: 'Вопросов:',
    timeLimitLabel: 'Лимит времени:',
    passingScoreLabel: 'Проходной балл:',
    topLeaders: '👑 Топ 3 лидера',
    anonymous: 'Аноним',
    startQuiz: 'НАЧАТЬ КВИЗ →',
    resumingSession: 'Восстановление сессии...',
    loadingQuiz: 'Загрузка квиза...',
    startingQuiz: 'Запуск квиза...',
    questionOf: 'Вопрос {current} из {total}',
    score: 'Очки: {score}',
    streakLabel: '🔥 {count} подряд!',
    correct: '✓ Правильно!',
    pointsEarned: '+{points} очков',
    basePoints: 'Базовые: {points}',
    speedBonus: 'Бонус скорости: +{points} ⚡',
    streakBonus: 'Бонус серии: +{points} 🔥',
    wrongAnswer: 'В следующий раз повезёт! Серия прервана.',
    streakBonusTitle: '🔥 Бонус серии!',
    streakBonusDesc: 'Отлично! {count} правильных ответов подряд! +{bonus} бонусных очков',
    submit: 'Ответить',
    submitting: 'Отправка...',
    loadingNext: 'Загрузка следующего вопроса...',
    loadFailed2: 'Не удалось загрузить квиз',
    tryAgain2: 'Попробуйте ещё раз',
    activeSession: 'Найдена активная сессия',
    activeSessionDesc: 'У вас уже есть активная сессия. Продолжить с места остановки или начать заново?',
    continueSession: 'Продолжить сессию',
    startFresh: 'Начать заново',
    startingFresh: 'Начинаем заново...',
    loadingResults: 'Загрузка результатов...',
    loadResultsFailed: 'Не удалось загрузить результаты',
    completed: 'Квиз завершён!',
    passed: '✓ Пройден',
    notPassed: '✗ Не пройден',
    correctAnswers: 'Правильных ответов',
    timeSpent: 'Время',
    pointsEarnedLabel: 'Набрано очков',
    accuracy: 'Точность',
    longestStreak: 'Лучшая серия',
    avgResponse: 'Ср. ответ',
    tryAgainBtn: 'Попробовать снова',
    viewLeaderboard: 'Смотреть рейтинг',
    backToHome: 'На главную',
    shareAchievement: 'Поделись достижением!',
    share: 'Поделиться',
  },
  profile: {
    title: 'Профиль',
    email: 'Email',
    notSet: 'Не указан',
    language: 'Язык',
    statistics: 'Статистика',
    quizzesCompleted: 'Квизов пройдено',
    totalPoints: 'Всего очков',
    averageScore: 'Средний балл',
    bestRank: 'Лучший ранг',
    achievements: 'Достижения',
    achievementsSoon: 'Система достижений скоро появится!',
    recentActivity: 'Последняя активность',
    activitySoon: 'История активности скоро появится!',
  },
  leaderboard: {
    title: 'Рейтинг',
    subtitle: 'Лучшие игроки по очкам',
    loading: 'Загрузка рейтинга...',
    loadFailed: 'Не удалось загрузить рейтинг',
    retry: 'Повторить',
    empty: 'Пока нет результатов!',
    emptyDesc: 'Станьте первым, кто пройдёт этот квиз.',
    yourRank: 'Ваш ранг',
    colRank: 'Место',
    colPlayer: 'Игрок',
    colScore: 'Очки',
    colDate: 'Дата',
    anonymous: 'Аноним',
    showingTop: 'Топ {count} игроков',
  },
  categories: {
    title: 'Quiz Sprint',
    subtitle: 'Выберите категорию',
    loading: 'Загрузка категорий...',
    loadError: 'Ошибка загрузки',
    loadFailed: 'Не удалось загрузить категории',
    tryAgain: 'Попробовать снова',
    exploreDesc: 'Квизы по теме {name}',
    empty: 'Категории пока не доступны',
    emptyDesc: 'Убедитесь что backend запущен',
  },
  marathon: {
    title: 'Марафон',
    score: 'Очки',
    question: 'Вопрос',
    recordLabel: '{score}/{best} рекорд',
    gameOver: 'Игра окончена',
    questionsAnswered: '{count} вопросов отвечено',
    personalBest: 'Рекорд',
    bonuses: 'Бонусы',
    energyRule1: '5 энергии, ошибка = −1 ⚡',
    energyRule2: '5 правильных подряд = +1 ⚡',
    difficultyRule: 'Сложность растёт со временем',
    continueMarathon: 'Продолжить марафон',
    newRun: 'Новый забег',
    viewResults: 'Смотреть результаты',
    startMarathon: 'Начать марафон',
    nextMilestone: 'Следующий рубеж: {next} ({remaining} осталось)',
    loadingQuestion: 'Загрузка вопроса...',
    submittingAnswer: 'Отправка ответа...',
    shieldAbsorbed: 'Щит поглотил удар!',
    correct: 'Правильно!',
    wrongLifeLost: 'Неправильно! Жизнь потеряна',
    incorrect: 'Неправильно',
    runCompleted: 'Забег завершён',
    correctAnswers: 'Правильных ответов',
    questions: 'Вопросов',
    newRecord: 'Новый рекорд! 🎉',
    continueRun: 'Продолжить текущий забег?',
    continueRunDesc: 'Получи +1 ⚡ и продолжи с того же места',
    continueWithCoins: 'Продолжить ({coins} монет)',
    watchAd: 'Смотреть рекламу',
    home: 'На главную',
    chooseCategory: 'Выберите категорию',
    chooseCategoryDesc: 'Выберите категорию для марафона',
    loadingCategories: 'Загрузка категорий...',
    allCategories: 'Все категории',
    allCategoriesDesc: 'Вопросы по всем темам',
    lives: '{count} жизней',
  },
  daily: {
    title: 'Вызов дня',
    continue: 'Продолжить',
    viewResults: 'Смотреть результаты',
    startChallenge: 'Начать вызов',
    questionsInfo: '10 вопросов • 15с каждый',
    resetsIn: 'Обновится через',
    playersToday: 'Игроков сегодня',
    topPlayers: 'Лучшие игроки',
    todaysChallenge: 'Вызов дня',
    noPlayers: 'Пока нет игроков',
    noPlayersDesc: 'Станьте первым, кто пройдёт сегодняшний вызов!',
    youBadge: 'Вы',
    points: 'очков',
    loadingQuestion: 'Загрузка вопроса...',
    submitting: 'Отправка ответа...',
    correct: 'Правильно!',
    incorrect: 'Неправильно',
    questionOf: '{current}/{total}',
    loadingResults: 'Загрузка результатов...',
    baseScore: 'База: {score}',
    streakBonus: '+ {bonus} серия (×{multiplier})',
    correctOf: '{correct} / {total} правильных',
    scorePercent: '({percent}%)',
    newStreakRecord: 'Новый рекорд серии!',
    streakRecordDesc: 'Вы достигли серии в {days} дней!',
    yourRewards: 'Ваши награды',
    coins: 'Монеты',
    pvpTickets: 'PVP билеты',
    streakApplied: 'Серия ×{multiplier} применена к монетам',
    marathonBonuses: 'Бонусы марафона',
    shieldName: 'Щит',
    shieldDesc: 'Поглощает 1 неправильный ответ',
    fiftyfiftyName: '50/50',
    fiftyfiftyDesc: 'Убирает 2 неправильных ответа',
    skipName: 'Пропуск',
    skipDesc: 'Пропустить без штрафа',
    freezeName: 'Заморозка',
    freezeDesc: '+5 секунд к таймеру',
    bonusPlus1: '+1',
    bonusUseDesc: 'Используй в режиме Марафона',
    yourRanking: 'Ваш рейтинг',
    yourRank: 'Ваш ранг',
    totalPlayers: 'Всего игроков',
    tryAgain: 'Попробовать снова?',
    tryAgainDesc: 'Вы можете пройти сегодняшний вызов ещё раз для улучшения результата. В рейтинг идёт лучший результат.',
    retryCoins: '100 монет',
    retry: 'Повторить',
    watchAd: 'Смотреть рекламу',
    freeRetry: 'Бесплатно',
    backToHome: 'На главную',
    nextChallenge: 'Следующий вызов через',
    loadingReview: 'Загрузка разбора...',
    reviewTitle: 'Разбор ответов',
    reviewDesc: 'Посмотрите свои ответы из сегодняшнего вызова',
    correctLabel: 'Правильно',
    wrongLabel: 'Неправильно',
    backToResults: 'К результатам',
    yourAnswer: 'Ваш ответ:',
    correctAnswer: 'Правильный ответ:',
    pointsEarned: '+{points} очков',
    noPoints: 'Нет очков',
  },
  duel: {
    title: 'PvP Дуэль',
    acceptingChallenge: 'Принимаем вызов...',
    pleaseLogin: 'Пожалуйста, авторизуйтесь',
    acceptFailed: 'Не удалось принять вызов. Возможно, ссылка устарела.',
    close: 'Закрыть',
    tickets: 'билетов',
    wins: 'Победы',
    losses: 'Поражения',
    winRate: 'Процент побед',
    pendingChallenges: 'Ожидающие вызовы',
    challenge: 'Вызов',
    accept: 'Принять',
    decline: 'Отклонить',
    tabPlay: 'Играть',
    tabLeaderboard: 'Рейтинг',
    tabHistory: 'История',
    searching: 'Ищем соперника...',
    cancel: 'Отмена',
    findOpponent: 'Найти случайного соперника',
    noTickets: 'Нет билетов',
    challengeFriend: 'Вызвать друга',
    inviteFriend: 'Пригласить в Telegram',
    createLink: 'Создать ссылку',
    shareLink: 'Поделитесь ссылкой:',
    copy: 'Копировать',
    friendsOnline: 'Друзья онлайн',
    inGameBadge: 'В игре',
    yourRank: 'Ваш ранг',
    player: 'Игрок',
    wIndicator: 'П',
    lIndicator: 'П',
    noGames: 'Нет игр',
    vsOpponent: 'vs {name}',
    friendBadge: 'Друг',
    pendingCount: '{count} вызовов',
    mmrValue: '{mmr} MMR',
    thisSeason: 'В этом сезоне',
    totalGames: '{count} игр',
    continueDuel: 'Продолжить дуэль',
    challengesCount: 'Вызовы ({count})',
    reconnecting: 'Переподключение...',
    connecting: 'Подключение...',
    waitingOpponent: 'Ожидаем соперника...',
    waitingDesc: 'Дуэль начнётся, когда оба игрока будут готовы',
    getReady: 'Приготовься!',
    you: 'Вы',
    round: 'Раунд',
    roundOf: '{current}/{total}',
    draw: 'Ничья!',
    victory: 'Победа!',
    defeat: 'Поражение',
    mmrChange: '{sign}{amount} MMR',
    gameResult: 'Результат игры',
    opponent: 'Соперник',
    friendGame: 'Игра с другом',
    requestRematch: 'Запросить реванш',
    shareResult: 'Поделиться результатом',
    backToLobby: 'В лобби',
  },
  shared: {
    questionOf: 'Вопрос {current} / {total}',
    pts: '{pts} очков',
    timesUp: 'Время вышло!',
  },
}
```

**Step 2: Run type-check**

```bash
cd tma && pnpm type-check
```

Expected: No errors.

**Step 3: Commit**

```bash
git add tma/src/i18n/locales/ru.ts
git commit -m "feat(i18n): add Russian translation strings"
```

---

## Task 4: Wire i18n into main.ts and App.vue

**Files:**
- Modify: `tma/src/main.ts`
- Modify: `tma/src/App.vue`

**Step 1: Update `src/main.ts`**

Add `i18n` plugin registration. The import goes right after the existing imports. The `app.use(i18n)` goes **before** `app.use(ui)`:

```typescript
// Add to imports:
import { i18n } from './i18n/index'

// In initializeApp(), before app.use(router):
app.use(i18n)
```

Full updated section (replace lines 42–45 in current main.ts):

```typescript
// 5. Подключаем плагины
app.use(router)
app.use(i18n)
app.use(ui)
app.use(VueQueryPlugin, vueQueryOptions)
```

**Step 2: Update `src/App.vue` — wire locale to UApp**

Add imports and computed locale at the top of `<script setup>`:

```typescript
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'
import * as nuxtLocales from '@nuxt/ui/locale'

const { t, locale } = useI18n()
const nuxtLocale = computed(() => nuxtLocales[locale.value as keyof typeof nuxtLocales] ?? nuxtLocales.en)
```

Update the `<UApp>` tag:

```vue
<UApp :locale="nuxtLocale">
```

Replace hardcoded strings in `App.vue` template with `t()` calls:

| Current | Replace with |
|---------|-------------|
| `<p>Загрузка...</p>` | `<p>{{ t('app.loading') }}</p>` |
| `<h2>Ошибка</h2>` | `<h2>{{ t('app.error') }}</h2>` |
| `error.value = 'Приложение должно быть запущено в Telegram'` | `error.value = t('app.openInTelegram')` |
| `error.value = 'Нет данных авторизации из Telegram'` | `error.value = t('app.noAuthData')` |
| `error.value = 'Не удалось зарегистрировать пользователя'` | `error.value = t('app.registerFailed')` |
| `<p class="hint">Убедитесь, что приложение запущено в Telegram</p>` | `<p class="hint">{{ t('app.openInTelegramHint') }}</p>` |

**Note:** The error strings in `onMounted` are set as JS strings, not template text. Use `t()` directly in the script, but `t()` is only available after `useI18n()` is called. Since `App.vue` uses `<script setup>`, `useI18n()` is called at setup time and is available in `onMounted`.

**Step 3: Run dev server to verify no runtime errors**

```bash
cd tma && pnpm dev
```

Expected: App starts without errors. Open `https://dev.quiz-sprint-tma.online` and verify loading spinner shows.

**Step 4: Commit**

```bash
git add tma/src/main.ts tma/src/App.vue
git commit -m "feat(i18n): wire vue-i18n into app entry point and UApp locale"
```

---

## Task 5: Translate BottomTabBar and shared components

**Files:**
- Modify: `tma/src/components/BottomTabBar.vue`
- Modify: `tma/src/components/shared/QuestionCard.vue`
- Modify: `tma/src/components/shared/GameTimer.vue`
- Modify: `tma/src/components/shared/GameModeCard.vue`

**Step 1: BottomTabBar.vue**

Add to script: `import { useI18n } from 'vue-i18n'` and `const { t } = useI18n()` in `<script setup>`.

Replace hardcoded labels:
- `'Home'` → `t('nav.home')`
- `'Leaderboard'` → `t('nav.leaderboard')`
- `'Profile'` → `t('nav.profile')`

If labels are in a `tabs` array in `<script setup>`, update those computed values using `t()`. If they're hardcoded in template props like `:label="'Home'"`, change to `:label="t('nav.home')"`.

**Step 2: QuestionCard.vue**

Add `const { t } = useI18n()`.

Replace:
- `'Question {{ questionNumber }} / {{ totalQuestions }}'` → `t('shared.questionOf', { current: questionNumber, total: totalQuestions })`
- `'{{ points }} pts'` → `t('shared.pts', { pts: points })`

**Step 3: GameTimer.vue**

Add `const { t } = useI18n()`.

Replace `"Time's up!"` → `t('shared.timesUp')`

**Step 4: GameModeCard.vue**

Add `const { t } = useI18n()`.

Find the lives indicator text (`'{{ lives }} lives'`) and replace with `t('marathon.lives', { count: lives })`.

**Step 5: Verify build compiles**

```bash
cd tma && pnpm type-check
```

**Step 6: Commit**

```bash
git add tma/src/components/BottomTabBar.vue tma/src/components/shared/
git commit -m "feat(i18n): translate shared components and nav"
```

---

## Task 6: Translate HomeView and CategoriesView

**Files:**
- Modify: `tma/src/views/HomeView.vue`
- Modify: `tma/src/views/CategoriesView.vue`

**Step 1: HomeView.vue**

Add `const { t } = useI18n()` (after existing imports).

Replacements:
- `"Today's Challenge"` → `t('home.todaysChallenge')`
- `"Game Modes"` → `t('home.gameModes')`
- `title="Party Mode"` → `:title="t('home.partyMode')"`
- `description="Multiplayer quiz party with friends"` → `:description="t('home.partyModeDesc')"`
- `badge="Coming Soon"` → `:badge="t('home.comingSoon')"`
- `title="Tournament"` → `:title="t('home.tournament')"`
- `description="Compete in weekly tournaments for prizes"` → `:description="t('home.tournamentDesc')"`

**Step 2: CategoriesView.vue**

Add `const { t } = useI18n()`.

Replacements (find exact strings in file and replace):
- `'Quiz Sprint'` → `t('categories.title')`
- `'Выберите категорию'` → `t('categories.subtitle')`
- `'Загрузка категорий...'` → `t('categories.loading')`
- `'Ошибка загрузки'` → `t('categories.loadError')`
- `'Не удалось загрузить категории'` → `t('categories.loadFailed')`
- `'Попробовать снова'` → `t('categories.tryAgain')`
- `` `Explore ${category.name.toLowerCase()} quizzes` `` → `t('categories.exploreDesc', { name: category.name.toLowerCase() })`
- `'Категории пока не доступны'` → `t('categories.empty')`
- `'Убедитесь что backend запущен'` → `t('categories.emptyDesc')`

**Step 3: Commit**

```bash
git add tma/src/views/HomeView.vue tma/src/views/CategoriesView.vue
git commit -m "feat(i18n): translate HomeView and CategoriesView"
```

---

## Task 7: Translate Quiz views (List, Details, Play, Results)

**Files:**
- Modify: `tma/src/views/QuizListView.vue`
- Modify: `tma/src/views/QuizDetailsView.vue`
- Modify: `tma/src/views/QuizPlayView.vue`
- Modify: `tma/src/views/QuizResultsView.vue`

**Step 1: QuizListView.vue** — add `const { t } = useI18n()`, replace all strings using `quiz.*` keys.

Key replacements:
- `'Квизы'` → `t('quiz.title')`
- `` `Категория: ${categoryName}` `` → `t('quiz.category', { name: categoryName })`
- `'Загрузка квизов...'` → `t('quiz.loading')`
- `'Ошибка загрузки'` → `t('quiz.loadError')`
- `'Не удалось загрузить квизы'` → `t('quiz.loadFailed')`
- `'Попробовать снова'` → `t('quiz.tryAgain')`
- `'Unnamed Quiz'` → `t('quiz.unnamed')`
- `'No description'` → `t('quiz.noDescription')`
- `` `📝 ${quiz.questionsCount || 0} вопросов` `` → `` `📝 ${t('quiz.questionsCount', { count: quiz.questionsCount || 0 })}` ``
- `` `⏱️ ${Math.floor(quiz.timeLimit / 60)} мин` `` → `` `⏱️ ${t('quiz.timeLimit', { min: Math.floor(quiz.timeLimit / 60) })}` ``
- `` `✅ Проходной балл: ${quiz.passingScore || 0}%` `` → `t('quiz.passingScore', { score: quiz.passingScore || 0 })`
- `'View Quiz'` → `t('quiz.viewQuiz')`
- `'Квизы не найдены'` → `t('quiz.notFound')`
- `'В этой категории пока нет квизов'` → `t('quiz.notFoundDesc')`
- `'Вернуться к категориям'` → `t('quiz.backToCategories')`

**Step 2: QuizDetailsView.vue** — add `const { t } = useI18n()`, replace all strings using `quiz.*` keys.

Key replacements:
- `'Loading quiz details...'` → `t('quiz.loadingQuiz')`
- `'Error loading quiz'` → `t('quiz.loadError')`
- `'Failed to load quiz details'` → `t('quiz.loadFailed')`
- `'📊 Quiz Stats'` → `t('quiz.stats')`
- `'Questions:'` → `t('quiz.questions')`
- `'Time Limit:'` → `t('quiz.timeLimitLabel')`
- `'Passing Score:'` → `t('quiz.passingScoreLabel')`
- `'👑 Top 3 Leaders'` → `t('quiz.topLeaders')`
- `'Anonymous'` → `t('quiz.anonymous')`
- `'START QUIZ →'` → `t('quiz.startQuiz')`

**Step 3: QuizPlayView.vue** — add `const { t } = useI18n()`.

Key replacements (check actual file for exact usage patterns):
- `'Resuming session...'` → `t('quiz.resumingSession')`
- `'Loading quiz...'` → `t('quiz.loadingQuiz')`
- `'Starting quiz...'` → `t('quiz.startingQuiz')`
- Template: `'Question {{ currentQuestionIndex }} of {{ totalQuestions }}'` → `t('quiz.questionOf', { current: currentQuestionIndex, total: totalQuestions })`
- `'Score: {{ session.score }}'` → `t('quiz.score', { score: session.score })`
- For streak/correct/bonus feedback messages — replace with `t()` + interpolation params
- `'Submit Answer'` / `'Submitting...'` (ternary) → use a computed: `isSubmitting ? t('quiz.submitting') : t('quiz.submit')`
- `'Active Quiz Session Found'` → `t('quiz.activeSession')`
- `'Continue Session'` → `t('quiz.continueSession')`
- `'Start Fresh'` / `'Starting Fresh...'` → ternary with `t()`

**Step 4: QuizResultsView.vue** — add `const { t } = useI18n()`.

Replace all result screen strings using `quiz.*` keys.

**Step 5: Type-check**

```bash
cd tma && pnpm type-check
```

**Step 6: Commit**

```bash
git add tma/src/views/QuizListView.vue tma/src/views/QuizDetailsView.vue tma/src/views/QuizPlayView.vue tma/src/views/QuizResultsView.vue
git commit -m "feat(i18n): translate quiz views"
```

---

## Task 8: Translate ProfileView (+ add language switcher) and LeaderboardView

**Files:**
- Modify: `tma/src/views/ProfileView.vue`
- Modify: `tma/src/views/LeaderboardView.vue`

**Step 1: ProfileView.vue — add language switcher**

Add to `<script setup>`:

```typescript
import { useI18n } from 'vue-i18n'
import { setLocale, type Locale } from '@/i18n/index'

const { t, locale } = useI18n()

const localeOptions = [
  { label: 'English', value: 'en' },
  { label: 'Русский', value: 'ru' },
]

function handleLocaleChange(val: string) {
  setLocale(val as Locale)
}
```

Replace the existing Language section in template:

```vue
<div>
  <p class="text-sm text-gray-600">{{ t('profile.language') }}</p>
  <USelect
    :model-value="locale"
    :items="localeOptions"
    @update:model-value="handleLocaleChange"
  />
</div>
```

Replace all other hardcoded strings using `profile.*` keys.

**Step 2: LeaderboardView.vue**

Add `const { t } = useI18n()`.

Replace all strings using `leaderboard.*` keys.

**Step 3: Type-check and verify switcher works**

```bash
cd tma && pnpm type-check
```

Open app → Profile → change language → verify all visible text switches instantly.

**Step 4: Commit**

```bash
git add tma/src/views/ProfileView.vue tma/src/views/LeaderboardView.vue
git commit -m "feat(i18n): translate ProfileView + LeaderboardView, add language switcher"
```

---

## Task 9: Translate Marathon views and component

**Files:**
- Modify: `tma/src/components/Marathon/MarathonCard.vue`
- Modify: `tma/src/views/Marathon/MarathonPlayView.vue`
- Modify: `tma/src/views/Marathon/MarathonGameOverView.vue`
- Modify: `tma/src/views/Marathon/MarathonCategoryView.vue`

Add `const { t } = useI18n()` to each file's `<script setup>` and replace all hardcoded strings using `marathon.*` keys per the English/Russian translation tables in Tasks 2 and 3.

Key patterns:
- `'Марафон'` / `'Marathon'` → `t('marathon.title')`
- `'Новый забег'` / `'New Run'` → `t('marathon.newRun')`
- `'Личный рекорд'` → `t('marathon.personalBest')`
- `` `Next milestone: ${state.milestone.next} (${state.milestone.remaining} to go)` `` → `t('marathon.nextMilestone', { next: state.milestone.next, remaining: state.milestone.remaining })`
- `'Продолжить ({{ continueOffer.costCoins }} монет)'` → `t('marathon.continueWithCoins', { coins: continueOffer.costCoins })`
- Lives indicator in GameModeCard: handled in Task 5

**Commit:**

```bash
git add tma/src/components/Marathon/ tma/src/views/Marathon/
git commit -m "feat(i18n): translate Marathon views and component"
```

---

## Task 10: Translate Daily Challenge views and components

**Files:**
- Modify: `tma/src/components/DailyChallenge/DailyChallengeCard.vue`
- Modify: `tma/src/components/DailyChallenge/DailyChallengeLeaderboard.vue`
- Modify: `tma/src/components/DailyChallenge/DailyChallengeReviewAnswer.vue`
- Modify: `tma/src/views/DailyChallenge/DailyChallengePlayView.vue`
- Modify: `tma/src/views/DailyChallenge/DailyChallengeResultsView.vue`
- Modify: `tma/src/views/DailyChallenge/DailyChallengeReviewView.vue`

Add `const { t } = useI18n()` to each file and replace all hardcoded strings using `daily.*` keys.

Key patterns:
- `"Today's Challenge"` → `t('daily.title')`
- `'Start Challenge'` → `t('daily.startChallenge')`
- `'10 questions • 15s each'` → `t('daily.questionsInfo')`
- `'+ {{ results.streakBonus }} streak (×{{ streakMultiplier }})'` → `t('daily.streakBonus', { bonus: results.streakBonus, multiplier: streakMultiplier })`
- `"You've reached a {{ streak!.currentStreak }} day streak!"` → `t('daily.streakRecordDesc', { days: streak!.currentStreak })`
- For bonus items (Shield, 50/50, Skip, Freeze) — replace name and description props with `t()` calls

**Commit:**

```bash
git add tma/src/components/DailyChallenge/ tma/src/views/DailyChallenge/
git commit -m "feat(i18n): translate Daily Challenge views and components"
```

---

## Task 11: Translate Duel views and component

**Files:**
- Modify: `tma/src/components/Duel/DuelCard.vue`
- Modify: `tma/src/views/Duel/DuelLobbyView.vue`
- Modify: `tma/src/views/Duel/DuelPlayView.vue`
- Modify: `tma/src/views/Duel/DuelResultsView.vue`

Add `const { t } = useI18n()` to each file and replace all strings using `duel.*` keys.

Key patterns:
- `'PvP Duel'` → `t('duel.title')`
- `'Принимаем вызов...'` → `t('duel.acceptingChallenge')`
- `'Searching for opponent...'` → `t('duel.searching')`
- `'Waiting for opponent...'` → `t('duel.waitingOpponent')`
- `'Draw!'` / `'Victory!'` / `'Defeat'` → `t('duel.draw')`, `t('duel.victory')`, `t('duel.defeat')`
- MMR change display: `{{ myMmrChange >= 0 ? '+' : '' }}{{ myMmrChange }} MMR` →
  `t('duel.mmrChange', { sign: myMmrChange >= 0 ? '+' : '', amount: Math.abs(myMmrChange) })`
- `'{{ pendingChallenges.length }} challenges'` → `t('duel.pendingCount', { count: pendingChallenges.length })`

**Step: Final type-check and lint**

```bash
cd tma && pnpm type-check && pnpm lint
```

Expected: No errors.

**Commit:**

```bash
git add tma/src/components/Duel/ tma/src/views/Duel/
git commit -m "feat(i18n): translate Duel views and component"
```

---

## Task 12: Final verification

**Step 1: Run full test suite**

```bash
cd tma && pnpm test:unit
```

Expected: All tests pass including the new i18n detection tests.

**Step 2: Build for production**

```bash
cd tma && pnpm build
```

Expected: Clean build with no TypeScript errors.

**Step 3: Manual smoke test**

- Open app → all English text visible
- Profile → change to Русский → all text switches to Russian
- Refresh page → Russian persists (localStorage)
- Nuxt UI internal strings (if any pagination/modals visible) also switch

**Step 4: Push branch**

```bash
git push origin add-i18n
```
